
**    std::list<LinkLine> linkLines;

    int animationRemain[Prop::animationAmount];
    bool singlePlayer;

    int timeRemain; 
    bool stop;
    bool **map;

    bool gameover;

 **   std::vector<Player> players;
 **   std::list<std::shared_ptr<Block>> blocks;
 **   std::list<Prop> props;

    std::unique_ptr<Hint> hint;


**    std::set<Point> emptySpace;
 **   std::set<Block> blocksAccessible;
 **   std::set<Solution> solutions;
 
 
 
 只要涉及到指针 均不能保存 需要看内存分配的问题
 
 
 所有需要保存的类增加两个
 1 重载输入输出 只保存需要的信息(拆解成员变量)
 2 默认构造函数 所有参数设置初始化